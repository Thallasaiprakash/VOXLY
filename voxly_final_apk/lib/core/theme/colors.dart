import 'package:flutter/material.dart';

class AppColors {
  // Core
  static const Color background = Color(0xFF080F1E); // Deep navy
  static const Color surface = Color(0xFF111928);    // Card background
  static const Color surface2 = Color(0xFF1A2438);   // Elevated surface

  // Accent
  static const Color primary = Color(0xFF3A7BD5);    // Electric blue
  static const Color primaryLight = Color(0xFF5A9BEA); // Light blue
  static const Color primaryGlow = Color(0xFF2A5FA8);  // Deep blue

  // Semantic
  static const Color success = Color(0xFF2ECC71);    // Task done
  static const Color warning = Color(0xFFF0A030);    // Due soon
  static const Color danger = Color(0xFFE05C5C);     // Overdue / critical

  // Text
  static const Color textPrimary = Color(0xFFE8EDF5); // Main text
  static const Color textSecond = Color(0xFF8899BB);  // Muted text
  static const Color textHint = Color(0xFF455575);    // Very muted
}
